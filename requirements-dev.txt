# BookingScraper Pro v6.0 — Development Dependencies
# ====================================================
# Install: pip install -r requirements.txt -r requirements-dev.txt

# --- Testing ---
pytest>=8.3.0,<8.4.0
pytest-asyncio>=0.25.0,<0.26.0
pytest-cov>=6.0.0,<6.1.0
pytest-mock>=3.14.0,<3.15.0
httpx>=0.28.0,<0.29.0          # FastAPI test client

# --- Code quality ---
ruff>=0.9.0,<0.10.0             # Linter + formatter (replaces flake8 + black)
mypy>=1.14.0,<1.15.0
types-requests>=2.32.0
types-beautifulsoup4>=4.12.0

# --- Complexity analysis ---
radon>=6.0.1,<6.1.0             # Cyclomatic complexity (audit BUG-001 tracking)
xenon>=0.9.1,<0.10.0

# --- Database test utilities ---
factory-boy>=3.3.0,<3.4.0
